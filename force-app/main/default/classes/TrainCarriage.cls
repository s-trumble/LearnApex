public with sharing class TrainCarriage {
    public Integer carriageIndex;
    public Integer passengerCount;
    public TrainCarriage(Integer index, Integer count) {
        this.carriageIndex = index;
        this.passengerCount = count;
    }
}